assert config != null
'PO channels where-used list'(config)

/**
 * Это отчёт показывает какой канал где используется
 * @param config
 * @return
 */
static 'PO channels where-used list'(Config config) {

}
